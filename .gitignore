*.ps1.bak
*.psm1.bak
*.psd1.bak
.vscode/
*.log
Temp/
*.tmp

# Test and coverage artifacts. Install-CopilotStandards.ps1 already writes these
# into the .gitignore it generates for consumers; this repo was missing them.
TestResults/
coverage.xml
Tests/Results/

# PlatyPS build intermediate. docs/ is source and is committed;
# maml/ is regenerated by Export-MamlCommandHelp on every build.
maml/
